#include <stdio.h>
#include "dinerdash.h"
#include "../../adt/string/string.h"

int main()
{
    int skor;
    dinerdash(&skor);

    // NOMOR(m) = 0;
    // DURASI(m) = 1;
    // KETAHANAN(m) = 5 + 1;
    // HARGA(m) = rand() % 5001 + 10000;
    // addOrder(&orderList, m);

    // NOMOR(m) = 1;
    // DURASI(m) = 2;
    // KETAHANAN(m) = 3;
    // HARGA(m) = rand() % 5001 + 10000;
    // addOrder(&orderList, m);

    // NOMOR(m) = 2;
    // DURASI(m) = 2;
    // KETAHANAN(m) = 1;
    // HARGA(m) = rand() % 5001 + 10000;

    // addOrder(&orderList, m);

    return 0;
}